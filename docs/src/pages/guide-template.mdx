---
title: Guide Template
description: Internal template for writing Crosshatch task guides with consistent goals, steps, examples, operations, and next steps.
searchable: false
---

import { Card, Cards } from "vocs"

# Replace Me

Shortly state what the reader will build, why it matters, and which Crosshatch boundary this guide covers.

## What You'll Build

Describe the end state in one or two paragraphs. Prefer concrete product language over protocol language.

## Prerequisites

- Crosshatch installed in the app or service
- An x402 payment requirement source
- Any runtime, framework, or merchant service assumptions

## Steps

::::steps

### Name The First Action

Explain the action and include the smallest useful code sample.

```ts
const example = "replace me"
```

### Name The Second Action

Show the next boundary in the flow. Keep snippets copy-pasteable when possible.

```ts
const next = "replace me"
```

### Verify The Result

Explain what success looks like and how the reader can tell the integration is working.

::::

## Complete Example

Include a single complete example when the guide has multiple snippets above.

```ts
export const example = async () => {
  return "replace me"
}
```

## Error Cases

Call out expected failure modes and how the integration should react.

- rejected approval
- missing payment payload
- invalid payment payload
- unsupported network or asset
- expired requirement or payload

## Observability

Describe trace spans, logs, and stable IDs that should exist in production.

- `x402.requirement.id`
- `x402.payload.id`
- `crosshatch.resource.id`
- `crosshatch.verification.result`

## Next Steps

<Cards>
  <Card
    title="Lifecycle"
    description="Connect this guide to the full Crosshatch payment lifecycle."
    icon="route"
    to="/lifecycle"
  />
  <Card
    title="OpenTelemetry"
    description="Trace paid requests across pricing, verification, settlement, and resource delivery."
    icon="activity"
    to="/merchants/opentelemetry"
  />
</Cards>
