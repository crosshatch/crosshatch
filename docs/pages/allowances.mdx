# Allowances

When a user first interacts with a Crosshatch-enabled app, they are asked for an
**allowance**-an app-scoped spending limit. The app can spend from this
allowance on behalf of the user without additional approvals. Allowances can be
rescinded anytime at [crosshatch.dev/rescind](crosshatch.dev/rescind).
