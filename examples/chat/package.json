{
  "name": "@crosshatch/chat",
  "version": "0.1.0-beta.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsc",
    "serve": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@crosshatch/config": "workspace:*",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
