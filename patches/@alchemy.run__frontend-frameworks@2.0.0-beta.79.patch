diff --git a/dist/waku/adapter.js b/dist/waku/adapter.js
index 702d55f..8c70e09 100644
--- a/dist/waku/adapter.js
+++ b/dist/waku/adapter.js
@@ -35,7 +35,7 @@ function removeGzipEncoding(res) {
 * `createServerEntryAdapter` so the emitted declaration never references
 * waku's non-exported `lib/types` module (TS2883).
 */
-const cloudflareAdapter = unstable_createServerEntryAdapter(({ processRequest, processBuild, setAllEnv, config, notFoundHtml }, options) => {
+const cloudflareAdapter = unstable_createServerEntryAdapter(({ processRequest, processBuild, unstable_setAllEnv, config, notFoundHtml }, options) => {
 	const { bodyLimit: bodyLimitOptions, middlewareFns = [], middlewareModules = {}, internalPathToBuildStaticFiles = "__waku_internal_build_static_files" } = options || {};
 	const app = new Hono();
 	app.notFound((c) => {
@@ -113,7 +113,7 @@ const cloudflareAdapter = unstable_createServerEntryAdapter(({ processRequest, p
 		defaultExport: {
 			...options?.handlers,
 			fetch(req, env) {
-				setAllEnv(env);
+				unstable_setAllEnv(env);
 				return fetchFn(req);
 			}
 		}
diff --git a/dist/waku/source.js b/dist/waku/source.js
index 36795bf..55b69b1 100644
--- a/dist/waku/source.js
+++ b/dist/waku/source.js
@@ -365,13 +365,16 @@ const makeWakuSourceProvider = (options, integration = {
 		}),
 		hash: Effect.fn(function* (_ctx, previous) {
 			const path = yield* Path.Path;
+			const fs = yield* FileSystem.FileSystem;
 			const rootDir = rootDirOf(path);
+			const previousWorkspaces = (previous?.additionalWorkspaces ?? []).map((workspace) => path.resolve(rootDir, workspace));
+			const workspaces = yield* Effect.filter(previousWorkspaces, (workspace) => fs.exists(workspace));
 			const input = yield* hashWakuInput({
 				rootDir,
 				distDir,
 				memo: options.memo,
 				options,
-				workspaces: (previous?.additionalWorkspaces ?? []).map((workspace) => path.resolve(rootDir, workspace))
+				workspaces
 			});
 			return {
 				input: input.hash,
diff --git a/src/waku/adapter.ts b/src/waku/adapter.ts
index facf2d5..c309042 100644
--- a/src/waku/adapter.ts
+++ b/src/waku/adapter.ts
@@ -98,7 +98,7 @@ const cloudflareAdapter: ReturnType<
   typeof createServerEntryAdapter<CloudflareAdapterOptions | undefined>
 > = createServerEntryAdapter(
   (
-    { processRequest, processBuild, setAllEnv, config, notFoundHtml },
+    { processRequest, processBuild, unstable_setAllEnv, config, notFoundHtml },
     options?: CloudflareAdapterOptions,
   ) => {
     const {
@@ -223,7 +223,7 @@ const cloudflareAdapter: ReturnType<
       defaultExport: {
         ...options?.handlers,
         fetch(req: Request, env: Record<string, unknown>) {
-          setAllEnv(env);
+          unstable_setAllEnv(env);
           return fetchFn(req);
         },
       },
diff --git a/src/waku/source.ts b/src/waku/source.ts
index 1cd52dd..a6404b9 100644
--- a/src/waku/source.ts
+++ b/src/waku/source.ts
@@ -818,15 +818,24 @@ export const makeWakuSourceProvider = (
     // only used for the auxiliary workspace list, never to skip recomputation.
     hash: Effect.fn(function* (_ctx, previous) {
       const path = yield* Path.Path;
+      const fs = yield* FileSystem.FileSystem;
       const rootDir = rootDirOf(path);
+      // A previously-discovered workspace may have been removed or renamed
+      // since the last build. Skip it rather than failing planning: dropping
+      // it changes the input hash, which forces the rebuild that re-discovers
+      // the current workspace set.
+      const previousWorkspaces = (previous?.additionalWorkspaces ?? []).map(
+        (workspace) => path.resolve(rootDir, workspace),
+      );
+      const workspaces = yield* Effect.filter(previousWorkspaces, (workspace) =>
+        fs.exists(workspace),
+      );
       const input = yield* hashWakuInput({
         rootDir,
         distDir,
         memo: options.memo,
         options,
-        workspaces: (previous?.additionalWorkspaces ?? []).map((workspace) =>
-          path.resolve(rootDir, workspace),
-        ),
+        workspaces,
       });
       return { input: input.hash, additionalWorkspaces: input.workspaces };
     }),
