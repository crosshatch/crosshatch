---
"@crosshatch/util": minor
"crosshatch": minor
---

Add structured extension envelopes, align facilitator API schemas with x402 responses, and fix Solana transaction construction.

Add the `PrintableAscii32` schema and remove the `JsonRecord` schema from `@crosshatch/util`.
